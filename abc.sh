#!/bin/bash

cat "$0" > abc.txt

#Outputs the contents of this bash script into a txt file named abc.txt in order to be referenced later

#can add a dir to output the txt file to if desired.

#Perms: chmod +x abc.sh
